
#! /usr/bin/env python
def build(bld):
    bld.program(features = 'cxx',
            source = 'new_experiment.cpp',
            includes = '. ../../',
            uselib = 'TBB BOOST EIGEN PTHREAD MPI',
            use = 'sferes2',
            target = 'dummy_run')
