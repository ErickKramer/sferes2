
#! /usr/bin/env python

from waflib.Configure import conf 

def options(opt):
    # Tools that will be loaded by waf
    # Tools are inside the directory waf_tools
    pass


def build(bld):
    bld.program(features = 'cxx',
            source = 'new_experiment.cpp',
            includes = '. ../../',
            uselib = 'TBB BOOST EIGEN PTHREAD MPI',
            use = 'sferes2',
            target = 'dummy_run')
